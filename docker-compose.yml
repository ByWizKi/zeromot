version: '3.9'

services:
  backend:
    build: ./backend
    volumes:
      - ./data:/data
    ports:
      - "8010:8000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3010:80"
    depends_on:
      - backend
    restart: unless-stopped
