{
  "buildCommand": "cd frontend && npm install && npm run build && mkdir -p ../api/static && cp -r dist/. ../api/static/",
  "functions": {
    "api/index.py": {
      "includeFiles": "api/static/**"
    }
  },
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
