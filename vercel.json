{
  "buildCommand": "cd frontend && npm install && npm run build && cd .. && mkdir -p api/static && cp -r frontend/dist/. api/static/",
  "routes": [
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
